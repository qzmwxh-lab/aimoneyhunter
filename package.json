{
  "name": "ai-money-hunter-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "build": "mkdir -p dist && cp index.html styles.css app.js dist/"
  }
}
